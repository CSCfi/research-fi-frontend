import { FilterSumPipe } from './filter-sum.pipe';

describe('FilterSumPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSumPipe();
    expect(pipe).toBeTruthy();
  });
});
